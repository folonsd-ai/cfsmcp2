# Convenience lockfile for Docker / legacy `pip install -r`.
# Canonical source: pyproject.toml [project.dependencies] — install with `pip install -e .`.
fastmcp>=3.0
zvec>=0.6.0
fastapi>=0.140
uvicorn[standard]>=0.30
httpx>=0.27
python-multipart>=0.0.9
pydantic-settings>=2.0
